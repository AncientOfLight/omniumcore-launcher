OmniumCore — Icons
==================

Replace `app_icon.svg` with your own .ico/.svg if desired.
For Windows you typically also want a `.ico` here (256x256 multi-res)
and reference it from a Windows .rc file added to CMakeLists.txt.
